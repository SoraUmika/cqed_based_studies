# Recovery Prompt - hybrid_qubit_cavity_control
Generated: 2026-03-24T07:13:49Z

---

## How to Use This File

1. Copy the **Agent Invocation** below and paste it into Copilot Chat.
2. The agent will read the state files listed under **Context Files** and
   continue from exactly where the loop left off.
3. Do NOT redo work that is already marked complete in TASK_CHECKLIST.md.

---

## Current Loop State

| Field            | Value                          |
|------------------|-------------------------------|
| Study            | hybrid_qubit_cavity_control          |
| Status           | INITIALIZED              |
| Phase            | needs-plan                        |
| Loop iteration   | 4 / 14           |
| Objective        | Extend the hybrid_qubit_cavity_control study into a speed-limit and implementation-feasibility analysis using D, qubit rotations, and SQR/CPSQR-style operations with fixed decomposition times t_R=16 ns and t_D=48 ns, then strengthen the physical stage with fair B_local coverage, adaptive sparse-tone selection, optimizer-backed refinement, and qutrit leakage replay. | EXTENSION: Investigate thermal photon effects on the best-performing SNAP and SQR gates           |

---

## Agent to Invoke

**Agent:** science-director  [model: o3]

Paste this into Copilot Chat:

`
@science-director study=studies/hybrid_qubit_cavity_control run=task_runs/hybrid_qubit_cavity_control phase=plan
`

---

## Context Files (read these first)

The agent MUST read these files before doing anything:

1. `studies/hybrid_qubit_cavity_control/study_state.json` - machine-readable study state
2. `task_runs/hybrid_qubit_cavity_control/SCIENCE_DIRECTIVE.md` - last planning directive
3. `task_runs/hybrid_qubit_cavity_control/EXECUTION_SUMMARY.md` - last execution results (if exists)
4. `task_runs/hybrid_qubit_cavity_control/TASK_CHECKLIST.md` - which tasks are done / open
5. `task_runs/hybrid_qubit_cavity_control/BLOCKERS.md` - known blockers
6. `studies/hybrid_qubit_cavity_control/IMPROVEMENTS.md` - improvement log

---

## Recovery Instructions for the Agent

This is a **recovery invocation** for a study that was interrupted or needs continuation.

- Current phase: **needs-plan**
- Iteration: 4 (max allowed: 14)
- Report preservation: ENABLED (append_iteration_section - do NOT overwrite report.tex)

Do NOT restart from scratch. Do NOT redo completed tasks. Read the context files
above, orient yourself to the current state, and continue the study forward.

## Active Blockers
- **P1.2 n_tr=3 inner loop**: `targeted_subspace_multitone` raises `ValueError: bloch_xyz_from_qubit_state requires a two-level reduced state` when `n_tr=3`. Framework limitation, not a configuration error.
- **P1.4 ConditionalPhaseSQR waveform bridge**: `waveform_sequence_from_gates` does not support ConditionalPhaseSQR gates. L2d sequences cannot be replayed.
- **Random SU(4) model-based synthesis**: `UnitarySynthesizer` with `model=model` triggers `_expand_target_matrix` shape mismatch (expects 4×4 or 16×16, gets misinterpreted). Ideal-only workaround applied.

## Open Tasks
- None remaining


---

## Loop Configuration (from research_config.json)

- Planning model:   o3
- Execution model:  claude-opus-4-6
- Max iterations:   14
- Blocked policy:   continue_with_partial
- Report mode:      append_iteration_section

---
*Generated by tools/research_loop.ps1 -Action recover -StudyName hybrid_qubit_cavity_control*